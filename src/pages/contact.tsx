import { Box, Typography } from "@mui/material";

export default function Contact() {
    return (
        <Box>
            <Typography className="page-headline" sx={{ typography: { sm: 'h1', xs: 'h6' } }}  variant="h1">Contact page</Typography>
        </Box>
    )
}